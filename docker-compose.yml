version: '3.8'
services:
  nexus:
    image: pg_backup
    build: .
    container_name: ${APP_NAME:-pg_database_backup}-pg-backup
    volumes:
      - "db_backup:/backups"
volumes:
  db_backup:
    external: true
