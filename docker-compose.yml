version: '3.8'
services:
  nexus:
    image: mysql_backup
    build: .
    container_name: ${APP_NAME:-mysql_database_backup}-mysql-backup
    volumes:
      - "mysql_db_backup:/backups"
volumes:
  mysql_db_backup:
    external: true
